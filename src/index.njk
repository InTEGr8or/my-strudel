<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Strudel Songs</title>
    <style>
        body {
            font-family: system-ui, -apple-system, sans-serif;
            background: #1a1a1a;
            color: #eee;
            margin: 2rem;
        }
        h1 { border-bottom: 2px solid #333; padding-bottom: 0.5rem; }
        ul { list-style: none; padding: 0; }
        li { margin: 1rem 0; }
        a { 
            display: block;
            padding: 1rem;
            background: #222;
            border-radius: 8px;
            color: #58a6ff;
            text-decoration: none;
            transition: background 0.2s;
        }
        a:hover { background: #333; }
        .folder {
            color: #888;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
    </style>
</head>
<body>
    <h1>My Strudel Songs</h1>
    <ul>
    {%- for song in collections.all -%}
      {%- if song.inputPath.endsWith('.strudel') or song.inputPath.endsWith('.tidal') -%}
        <li>
            <a href="{{ song.url }}">
                <div class="folder">
                    <span style="background:#444; padding:2px 4px; border-radius:3px; margin-right:5px; font-size:0.7rem; color:white;">
                        {{ song.inputPath.split('.').pop() | upper }}
                    </span>
                    {{ song.filePathStem | replace("/songs/", "") | replace(song.fileSlug, "") }}
                </div>
                <strong>{{ song.data.title }}</strong>
            </a>
        </li>
      {%- endif -%}
    {%- endfor -%}
    </ul>
</body>
</html>
